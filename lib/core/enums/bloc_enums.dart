enum ProductsEventType { getProducts, refreshProducts }

enum ProductDetailsEventType { getProductDetails, refreshProductDetails }

enum FavoriteEventType { checkFavorite, toggleFavorite }

enum ConnectivityEventType { checkConnectivity, connectivityChanged }

enum ConnectivityStatus { initial, checking, connected, disconnected }
